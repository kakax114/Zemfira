# Zemfira — Vinyl App

A single-page vinyl record collection browser. Open `vinyl-app.html` directly in any browser — no server needed.

---

## Run

```bash
open vinyl-app.html
```

Or double-click it in Finder.

---

## Notes

- No dependencies, no build step
- All styles and logic are self-contained in the HTML file
